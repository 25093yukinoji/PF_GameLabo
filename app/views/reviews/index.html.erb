<div class='row justify-content-around'>
  <div><%= link_to "レビュー新規投稿", new_game_review_path(@game), class: 'btn btn-success' %></div>
  <div class='mb-3'><%= link_to "戻る", game_path(@game), class:"btn btn-info" %></div>
</div>
<% if @reviews.present? %>
  <% @reviews.each do |review| %>
    <div class="row border-top  border-dark">
      <div class="col-md-6 mt-1 mb-1">
        <div class=" reviews-review">
          <p><%= review.created_at.strftime('%Y/%m/%d %H:%M') %></p>
          <h5 class="mb-3">評価：<%= review.rate %>点</h5>
          <!--星評価-->
         <div id="star-rate-<%= review.id %>"></div>
          <script>
          $('#star-rate-<%= review.id %>').raty({
            size: 36,
            starOff:  '<%= asset_path('star-off.png') %>',
            starOn : '<%= asset_path('star-on.png') %>',
            starHalf: '<%= asset_path('star-half.png') %>',
            half: true,
            readOnly: true,
            score: <%= review.rate %>,
          });
          </script>
        <!--/星評価-->
          <p><%= simple_format(review.body) %></p>
        </div>
      </div>
      <div class="col align-self-center edit-button">
        <% if current_user == review.user %>
          <%= link_to '口コミを削除', game_review_path(@game, review), method: :delete, class:'btn btn-danger' %>
        <% end %>
      </div>
    </div>
  <% end %>
<% end %>
<div class='row mt-5 justify-content-center'>
    <%= paginate @review %>
</div>